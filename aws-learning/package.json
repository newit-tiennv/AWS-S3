{
  "name": "aws-learning",
  "version": "1.0.0",
  "description": "1. Run command `docker compose up -d` 2. Go to `http://localhost:4566/health` to check service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-config": "^1.4.0"
  }
}
